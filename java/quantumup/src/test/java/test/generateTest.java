package test;


import org.junit.Test;
import za.ac.up.services.ExecutionFacade;
import za.ac.up.services.ReportGenerator;
import za.ac.up.services.ReportService;

import java.io.ByteArrayOutputStream;
import java.io.OutputStream;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;

public class generateTest {

    @Test
    public void test() throws Exception
    {
//        ReportService reportService = new ReportService();
//        reportService.setExecutionFacade(new ExecutionFacade());
//        reportService.setReportGenerator(new ReportGenerator());
//
//        OutputStream stream = reportService.getReport("a", "asfasfdasf", "SCATTER");
//
//        Path file = Paths.get("E:/Me/UP2018/COS730/quantumup/target/chart3.pdf");
//        Files.write(file, ((ByteArrayOutputStream) stream).toByteArray());
    }
}
